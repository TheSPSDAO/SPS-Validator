# Vite env vars must be prefixed with VALIDATOR_

# Option A (direct): Point the UI at a running validator API (must be reachable AND allow CORS)
# Examples:
# - Local validator: http://localhost:3333
# - Remote validator: https://your-host.example.com
VALIDATOR_API_URL=http://localhost:3333

# Option B (dev proxy): Use a same-origin base URL so the browser never sees cross-origin requests.
# Set the UI base to `/api` and set the proxy target to your remote validator API.
#
# VALIDATOR_API_URL=/api
# VALIDATOR_API_PROXY_TARGET=https://your-host.example.com
